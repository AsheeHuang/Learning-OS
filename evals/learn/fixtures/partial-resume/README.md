# Partial resume fixture

This fixture represents an interrupted `/learn-init Databases` setup:

- `MISSION.md` and `MAP.md` already contain human-authored state.
- `PROGRESS.md` contains one existing `Needs Validation` row.
- Other MAP concepts are missing from progress.
- `RESOURCES.md`, `HISTORY.md`, and artifact directories are absent.

A successful resume preserves existing bytes and learning state while adding only derivable missing artifacts and `Unexplored` progress rows.
